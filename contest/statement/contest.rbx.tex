\documentclass[a4paper,11pt]{article}

\usepackage{mineira}
\usepackage{import}

\def\Ano{\VAR{vars.year}}
\def\Modalidade{\VAR{contest.title}}
\def\Data{\VAR{vars.date}}

%- if is_editorial
\AddToHook{shipout/foreground}{
       \begin{tikzpicture}[overlay, remember picture]
            \node[text=gray, rotate=45, scale=8, opacity=0.5] at (current page) {EDITORIAL};
        \end{tikzpicture}
    }
%- endif

\begin{document}
\InsertTitlePage

%- for problem in problems
\stepcounter{problemcounter}
\subimport{\VAR{problem.path | parent}/}{\VAR{problem.path | stem}}
%- endfor

\label{lastpage}
\end{document}

